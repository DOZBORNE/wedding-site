<!--
	Never rendered — the load function in +page.server.ts always redirects to the
	main site, where the RSVP envelope lives. SvelteKit still wants a page
	component for the route to exist, so this is it.
-->
<p><a href="/#rsvp">Continue to the RSVP</a></p>
